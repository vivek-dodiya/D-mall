<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <header>
        <nav class=" bg-white w-full flex justify-between fixed top-0 left-0 px-5 py-3">
            <h2 class="text-2xl">D Mall <span class="text-blue-400 font-semibold text-sm">It'small But it's mall </span></h2>
            <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
            <% if(loggedin) { %>
                <div class="flex gap-5 ">
                <a href="/shop" class="hover:text-blue-400">Shop</a>
                <a href="/cart" class="hover:text-blue-400">Cart</a>
                <a href="/profile" class="hover:text-blue-400">MY account</a>
                <a href="/logout" class="text-red-400">Logout</a>
            </div>
            <% }%>   
            
        </nav>
    </header>
    
</body>

